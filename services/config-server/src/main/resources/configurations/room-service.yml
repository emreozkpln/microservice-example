spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/room
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql
server:
  port: 8070

application:
  config:
    lesson-url: http://localhost:8080/api/v1/lesson